local TARGET_FRAMERATE = 3

while true do
    game:GetService("RunService").RenderStepped:Wait()
    local t0 = tick()
    game:GetService("RunService").Heartbeat:Wait()
    repeat until t0 + 1/TARGET_FRAMERATE < tick()
end
